BEGIN CDI_DROP_OBJECT('CDI_PBS_CUSTOMER_LOOKUP','TABLE'); END;
/

CREATE TABLE CDI_PBS_CUSTOMER_LOOKUP
(
  BILL_ACCOUNT      VARCHAR2(10 BYTE),
  SERVICE_POINT     VARCHAR2(40 BYTE),
  STUDY_ID          VARCHAR2(4 BYTE),
  EFFECTIVE_DATE    DATE,
  TERMINATION_DATE  DATE
);

CREATE INDEX CDI_PBS_CUSTOMER_LOOKUP_IX01 ON CDI_PBS_CUSTOMER_LOOKUP(BILL_ACCOUNT, EFFECTIVE_DATE, TERMINATION_DATE);
