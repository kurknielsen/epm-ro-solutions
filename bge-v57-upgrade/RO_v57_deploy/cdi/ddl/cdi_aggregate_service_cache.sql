BEGIN CDI_DROP_OBJECT('CDI_AGGREGATE_SERVICE_CACHE','TABLE'); END;
/

CREATE TABLE CDI_AGGREGATE_SERVICE_CACHE
(
  AGGREGATE_ID     NUMBER(9),
  SERVICE_DATE     DATE,
  SERVICE_ACCOUNTS NUMBER(9),
  USAGE_FACTOR     NUMBER(14,6),
  CONSTRAINT PK_AGGREGATE_SERVICE_CACHE PRIMARY KEY(AGGREGATE_ID, SERVICE_DATE)
);
