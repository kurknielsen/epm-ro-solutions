DECLARE
CURSOR c_SELECT IS
   SELECT JOB_NAME
   FROM USER_SCHEDULER_JOBS
   WHERE UPPER(JOB_ACTION) LIKE '%BEGIN CDI_SCHEDULE_JOBS%';
BEGIN
   FOR v_SELECT IN c_SELECT LOOP
      DBMS_SCHEDULER.DROP_JOB(v_SELECT.JOB_NAME);
   END LOOP;
END;
/
