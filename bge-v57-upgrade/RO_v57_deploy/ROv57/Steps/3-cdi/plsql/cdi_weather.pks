CREATE OR REPLACE PACKAGE CDI_WEATHER AS

PROCEDURE RUN_INTERFACE;

PROCEDURE DEX_INTERFACE
   (
   p_IMPORT_FILE      IN CLOB,
   p_IMPORT_FILE_PATH IN VARCHAR2,
   p_STATUS          OUT NUMBER,
   p_MESSAGE         OUT VARCHAR2
   );

END CDI_WEATHER;
/
