BEGIN CDI_DROP_OBJECT('CDI_SERVICE_DATE_MAP', 'TABLE'); END;
/

CREATE GLOBAL TEMPORARY TABLE CDI_SERVICE_DATE_MAP
(
  INTERVAL_TYPE     VARCHAR2(32),
  SERVICE_DATE      DATE,
  SERVICE_INTERVAL  NUMBER(3),
  SERVICE_CUT_DATE  DATE,
  CONSTRAINT PK_SERVICE_DATE_MAP PRIMARY KEY (INTERVAL_TYPE, SERVICE_DATE, SERVICE_INTERVAL)
) ON COMMIT PRESERVE ROWS;
