CREATE OR REPLACE VIEW INTERCHANGE_TRANSACTION_TYPES ( TRANSACTION_TYPE,
 COMMODITY_TYPE) AS 
SELECT DISTINCT RTRIM(A.TRANSACTION_TYPE) || 's', B.COMMODITY_TYPE
FROM INTERCHANGE_TRANSACTION A,
	IT_COMMODITY B
WHERE A.COMMODITY_ID = B.COMMODITY_ID;
