CREATE OR REPLACE VIEW DISTINCT_PROSPECT_OFFERS AS 
SELECT DISTINCT  
	SCREEN_ID,  
	COMPARE_PRODUCT_ID,  
	PROFILE_CALENDAR_ID  
FROM PROSPECT  
WHERE COMPARE_PRODUCT_ID > 0  
  AND PROFILE_CALENDAR_ID > 0;
