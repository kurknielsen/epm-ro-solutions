Just run the script BUILD.SQL to do a MEX build.

This will create the tables, create all database objects, and compile all PL/SQL code.


To rebuild, you will need to run 'DropAllObjects.sql' prior to re-running BUILD.SQL