Just run the script BUILD.SQL to build the Market Interface portions of Market Operations.

This will create the tables, create all database objects, and compile all PL/SQL code (including
all required objects and PL/SQL code from the MEX folder)