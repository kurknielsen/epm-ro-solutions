DECLARE
    C NUMBER;
    BEGIN
        SELECT COUNT(*) INTO C FROM USER_SEQUENCES WHERE SEQUENCE_NAME = 'OASISTMPID';
        IF C = 0  THEN
        EXECUTE IMMEDIATE '
            CREATE SEQUENCE OASISTMPID
            INCREMENT BY 1
            START WITH 1';   
        END IF;
    END;
/