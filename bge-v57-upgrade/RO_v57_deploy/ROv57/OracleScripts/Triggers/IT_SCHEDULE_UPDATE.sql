CREATE OR REPLACE TRIGGER IT_SCHEDULE_UPDATE 
	BEFORE INSERT OR UPDATE ON IT_SCHEDULE 
	FOR EACH ROW 
BEGIN 
	IF NOT GA.VERSION_SCHEDULE THEN
		:new.AS_OF_DATE := LOW_DATE; 
	END IF; 
END IT_SCHEDULE_UPDATE;
/
