CREATE OR REPLACE TRIGGER AGGREGATE_ACCOUNT_SVC_UPDATE 
	BEFORE INSERT OR UPDATE ON AGGREGATE_ACCOUNT_SERVICE 
	FOR EACH ROW 
BEGIN 
	IF NOT GA.VERSION_AGGREGATE_ACCOUNT_SVC THEN
		:new.AS_OF_DATE := LOW_DATE; 
	END IF; 
END AGGREGATE_ACCOUNT_SVC_UPDATE;
/ 
