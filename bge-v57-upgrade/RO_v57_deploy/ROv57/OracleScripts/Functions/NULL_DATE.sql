CREATE OR REPLACE FUNCTION NULL_DATE
	(
	p_DATE IN DATE
	) RETURN DATE IS
--Revision: $Revision: 1.18 $
BEGIN
	IF p_DATE = CONSTANTS.HIGH_DATE THEN
		RETURN NULL;
	ELSE
		RETURN p_DATE;
	END IF;
END NULL_DATE;
/
