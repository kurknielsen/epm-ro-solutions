CREATE OR REPLACE VIEW ESP_POOL_INFO ( ESP_ID, 
POOL_ID, BEGIN_DATE, END_DATE, ENTRY_DATE, 
ESP_NAME ) AS SELECT B.ESP_ID, B.POOL_ID, B.BEGIN_DATE, B.END_DATE, B.ENTRY_DATE, A.ESP_NAME 
FROM ENERGY_SERVICE_PROVIDER A, ESP_POOL B 
WHERE A.ESP_ID = B.ESP_ID;
